const title = "Sign in for Jouve Manager";
const description = "Sign in for Jouve Manager";

export const metadata = {
  title,
  description,
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
